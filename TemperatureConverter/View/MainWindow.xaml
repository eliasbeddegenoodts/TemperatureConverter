<Window x:Class="View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:View"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Window.Resources>
        <LinearGradientBrush x:Key="labelBackgroundBrush" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="Gray" Offset="0" />
            <GradientStop Color="White" Offset="1" />
        </LinearGradientBrush>
    </Window.Resources>
    <StackPanel>
        <TextBlock Text="Celsius" FontWeight="Bold" Padding="4" FontSize="16" TextAlignment="Center" Background="{StaticResource labelBackgroundBrush}" />
        <TextBox x:Name="celciusTextBox" />
        <TextBlock Text="Fahrenheit" FontWeight="Bold" Padding="4" FontSize="16" TextAlignment="Center" Background="{StaticResource labelBackgroundBrush}" />
        <TextBox x:Name="fahrenheitTextbox" />
        <!-- Adding the x:Name="textBox" lets WPF generate a variable named textBox that contains a reference to the TextBox object-->
        <Button Content="Convert to Celsius" Click="ConvertToCelsius" />
        <Button Content="Convert to Fahrenheit" Click="ConvertToFahrenheit" />
    </StackPanel>
</Window>
